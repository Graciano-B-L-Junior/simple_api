services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: simple_api
      POSTGRES_USER: simple_api
      POSTGRES_PASSWORD: simple_api
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
